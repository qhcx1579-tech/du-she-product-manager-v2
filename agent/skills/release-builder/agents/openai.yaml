interface:
  display_name: "Release Builder"
  short_description: "发布前隐私审计、打包和 GitHub 指引。"
  default_prompt: "Use $release-builder to audit and prepare this project for release."
policy:
  allow_implicit_invocation: true
